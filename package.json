{
  "name": "@volta-oss/revenue-leak-finder",
  "version": "3.0.0",
  "private": false,
  "type": "module",
  "description": "A guided analysis workbook for spotting where a small business is losing sales online.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.js",
    "start": "python3 -m http.server 4173",
    "validate": "npm test",
    "certify": "npm run build && node --test test/v3.test.js",
    "test:all": "npm test",
    "clean": "rm -f src/*.js src/*.js.map src/*.d.ts test/*.js test/*.js.map test/*.d.ts",
    "ci": "npm ci && npm test && npm run test:backend --if-present",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "docker:build": "docker build -t volta-oss/revenue-leak-finder:local .",
    "docker:run": "docker compose up --build"
  },
  "keywords": [
    "digital-equity",
    "finance-grants",
    "local-first",
    "oss",
    "release",
    "volta"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/volta-npo/revenue-leak-finder.git"
  },
  "homepage": "https://volta-npo.github.io/revenue-leak-finder/",
  "bugs": {
    "url": "https://github.com/volta-npo/revenue-leak-finder/issues"
  },
  "author": "Volta Open Source <volta.npo@gmail.com>",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^26.0.0"
  }
}
